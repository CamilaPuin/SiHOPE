# URL base del backend para producción.
# En Vercel se recomienda sobreescribir esta variable desde el panel del proyecto
# (Settings → Environment Variables) apuntando a la URL pública del backend en Docker.
VITE_API_URL=https://api.sihope.com
